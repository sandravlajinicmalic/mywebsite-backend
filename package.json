{
  "name": "mywebsite-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "echo '=== Starting TypeScript compilation ===' && tsc --version && echo 'Running tsc...' && tsc 2>&1; if [ -f dist/index.js ]; then echo '✓ SUCCESS: dist/index.js generated'; exit 0; else echo '✗ ERROR: dist/index.js not found!'; exit 1; fi",
    "postinstall": "if [ ! -f dist/index.js ]; then echo 'dist/index.js not found, running build...' && npm run build || exit 1; else echo 'dist/index.js exists, skipping build'; fi",
    "postbuild": "test -f dist/index.js || (echo 'ERROR: dist/index.js not found after build!' && exit 1)",
    "deploy": "npm run build && eb deploy",
    "prestart": "pwd && echo 'Current directory:' && ls -la && echo 'Checking dist folder:' && ls -la dist/ 2>&1 || echo 'dist folder does not exist!'",
    "start": "if [ ! -f dist/index.js ]; then echo 'ERROR: dist/index.js not found! Attempting build...' && npm run build || exit 1; fi && node dist/index.js",
    "dev": "tsx watch index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend server za mywebsite sa Express, Supabase i Socket.io",
  "dependencies": {
    "@supabase/supabase-js": "^2.80.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.10.0",
    "@types/nodemailer": "^7.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.10",
    "openai": "^6.8.1",
    "socket.io": "^4.8.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
